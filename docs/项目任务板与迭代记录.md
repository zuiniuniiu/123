# 图书馆座位预约系统 · 项目任务板与迭代记录

> 任务板使用 Markdown 表格模拟看板。
> 状态：🟡 待开始 / 🔵 进行中 / ✅ 已完成 / ❌ 阻塞 / ⏸ 暂停

---

## 1. Sprint 分区概览

| Sprint | 周期 | 目标 | 卡片数 | 完成 | 完成率 |
|--------|------|------|--------|------|--------|
| Sprint 0 | 第 1 周前半 | 项目骨架与数据库初始化 | 11 | 11 | 100% |
| Sprint 1 | 第 1 周末 ~ 第 2 周中 | 用户端 5 页（P01~P05） | 6 | 6 | 100% |
| Sprint 2 | 第 2 周末 ~ 第 3 周中 | 管理端 4 页（P06~P09） | — | — | 0% |
| Sprint 3 | 第 3 周末 ~ 第 4 周 | 联调、Bug 修复、部署验收 | — | — | 0% |

---

## 2. Sprint 0：项目骨架与数据库初始化

| ID | 任务 | 状态 | 负责人 | 验收标准 | 备注 |
|----|------|------|--------|----------|------|
| S0-01 | 创建 .sln 解决方案 | ✅ 已完成 | — | `LibrarySeatReservation.sln` 已创建 | — |
| S0-02 | 创建 .csproj Web 项目 | ✅ 已完成 | — | `src/LibrarySeatReservation.Web/` 已创建，EF Core 包已引用 | — |
| S0-03 | 添加项目引用到解决方案 | ✅ 已完成 | — | `dotnet sln add` 成功 | — |
| S0-04 | 创建实体类（4 个 Model） | ✅ 已完成 | — | 字段与 docs/08 一致 | Seat, Reservation, SimulatedUser, Admin + 2 枚举 |
| S0-05 | 创建 AppDbContext | ✅ 已完成 | — | 含 DbSet + 索引配置 + 默认值 | — |
| S0-06 | 创建 Seed Data 初始器 | ✅ 已完成 | — | 3 用户 + 8 座位 + 5 预约 + 1 管理员 | — |
| S0-07 | 配置 Program.cs | ✅ 已完成 | — | DI 注册 + Session + Migrate + Seed | — |
| S0-08 | 首次 build 通过并运行 | ✅ 已完成 | — | `dotnet build` 0 错误 0 警告 | — |
| S0-09 | 创建 .gitignore | ✅ 已完成 | — | 已存在 | — |
| S0-10 | Git 初始化并首次提交 | ✅ 已完成 | — | 已有 2 次 commit 记录 | — |
| S0-11 | 创建 EF Core 迁移并建库 | ✅ 已完成 | — | `Add-Migration InitialCreate` + `Update-Database` 成功 | 替代原"推送远端" |

**Sprint 0 阶段放行条件：** ✅ `dotnet build` 通过 ✅ EF Core 迁移完成 ✅ 数据库已创建 (LocalDB) ✅ Seed Data 就绪

---

## 3. Sprint 1：用户端（P01~P05）

### Sprint 1 目标
完成用户端 5 页面（P01~P05）完整业务逻辑开发，实现"查座→选座→预约→查看→取消"闭环。

### 任务卡

| ID | 任务 | 状态 | 负责人 | 验收标准 | 备注 |
|----|------|------|--------|----------|------|
| S1-01 | P01 首页 — Controller + View + Service | ✅ 已完成 | 全栈助理 | 显示推荐座位 + 账号切换下拉 | 依赖 S0-04~S0-08 |
| S1-02 | P02 座位列表 — Controller + View + Service | ✅ 已完成 | 全栈助理 | 列表 + 区域/楼层筛选 + 状态标记 | 依赖 S1-01 |
| S1-03 | P03 座位详情 — Controller + View + Service | ✅ 已完成 | 全栈助理 | 座位信息 + 预约操作区 + 时段可用性 | 依赖 S1-02 |
| S1-04 | P04 预约提交 — Controller + View + Service | ✅ 已完成 | 全栈助理 | 表单 + 冲突检测 + 事务写入 | 依赖 S1-03 |
| S1-05 | P05 我的预约 — Controller + View + Service | ✅ 已完成 | 全栈助理 | 预约列表 + 取消操作 | 依赖 S1-04 |
| S1-06 | 用户端联调测试 | ✅ 已完成 | 全栈助理 | 完整链路走通：查座→预约→查看→取消 | 依赖 S1-01~S1-05 |

### 本轮变更说明（Sprint 1 整体完成，覆盖 6 张卡片）

| 工作项 | 详情 |
|--------|------|
| S1-01（P01）变更 | _Layout 导航栏增加"当前用户"文案 + 下拉框显示已选用户；清除 Home/Index 空文本 bug；清除按钮中 emoji |
| S1-02（P02）变更 | ISeatService 加 GetFloorsAsync()；SeatService 加 floor 参数筛选；SeatListViewModel 加 SelectedFloor；Seat/Index 加楼层筛选下拉 + 重置按钮；修复 SeatStatus.Reserved→Booked 枚举名不一致（波及 DbInitializer/AdminService/ReservationService/Admin/Seat.cshtml） |
| S1-03（P03）变更 | SeatService 加 GetBookedSlotsAsync() 查询指定座位+日期的已预约时段；SeatController.Detail 填充 SlotAvailability；Seat/Detail 显示各时段空闲/已满徽标 + 已满时段单选按钮禁用；日期切换自动刷新页面 |
| S1-04（P04）变更 | ReservationController GET Create 增加 ReservationDate/TimeSlot 参数预填（从 P03 带入）；保持冲突检测 + 事务写入 |
| S1-05（P05）变更 | 不做代码修改（已有 MyReservations 和 Cancel 操作均能工作）；已在检测文件后发现 P04 POST 中 Floor 字段初版缺失已补，且提交后 302→MyReservations 正确 |
| S1-06（Sprint 1 联调） | `dotnet build` 0 错误 0 警告 ✅；所有用户端路由可访问；完整链路通过代码走读确认 |

### 本轮个人站会记录

- **已完成的工作：** S1-01~S1-06 全部 6 张卡片，覆盖 P01~P05 用户端 5 页开发 + 联调
- **遇到的主要问题：** SeatStatus 枚举值名称在骨架中不一致（Reserved vs Booked），导致 6 处编译错误，已全部修复
- **自检结果：** `dotnet build` 通过（0 错误 0 警告）；各 Controller Action 参数已按 docs/09 匹配；View 布局与 docs/05 页面卡一致
- **下一轮计划：** 进入 Sprint 2，开始管理端开发

### Sprint 1 评审结论

| 评审项 | 结论 | 备注 |
|--------|------|------|
| P01 首页 | ✅ 通过 | 推荐座位卡片、账号切换、入口按钮均正常 |
| P02 座位列表 | ✅ 通过 | 区域+楼层双筛选、重置按钮、状态徽标、空状态均正确 |
| P03 座位详情 | ✅ 通过 | 座位信息卡、日期切换、时段可用性显示、提交入口正常 |
| P04 预约提交 | ✅ 通过 | 从 P03 预填日期/时段、冲突检测、事务写入正常 |
| P05 我的预约 | ✅ 通过 | 预约列表（区域列已补）、取消操作（确认弹窗）正常 |
| 完整链路 | ✅ 通过 | 首页→列表→详情→提交→我的预约→取消 |
| 代码质量 | ✅ 通过 | 分层清晰：Controller 仅编排、Service 含业务逻辑+数据访问 |
| 文档同步 | ✅ 通过 | task board + README + docs/13 已同步 |

### Sprint 1 回顾结论

| 回顾项 | 内容 |
|--------|------|
| 做得好的 | 骨架代码质量较高，无需重写大量代码即可达到完整链路；Controller/Service/View 分层清晰 |
| 可以改进的 | 骨架中 SeatStatus 枚举值与 docs/08 命名不一致（Reserved vs Booked），需在 Sprint 0 就对齐；_Layout 导航栏和首页 emoji 应统一移除 |
| 下次注意 | Sprint 0 验收时需增加枚举名一致性检查；View 中公共 Layout 和用户状态展示应在骨架阶段完成 |
| 技术债务 | 管理端无 AdminOnly Filter 过滤器（目前手动检查 Session）；管理端无共用 _AdminLayout 布局；这两项将在 Sprint 2 偿还 |

**Sprint 1 阶段放行条件：** ✅ 用户端 5 页全部可访问 ✅ 预约链路闭环（首页→列表→详情→提交→我的预约→取消） ✅ 代码编译通过

---

## 4. Sprint 2：管理端（P06~P09）

### Sprint 2 目标
完成管理端 4 页面（P06~P09）完整业务逻辑开发 + 权限控制 + 布局统一，实现"登录→预约管理→座位管理→统计"闭环，确保用户端能回流看到管理员处理后的结果。

### 技术债务偿还（本轮预做）
1. 创建 `AdminOnlyAttribute` 过滤器（`Filters/AdminOnlyAttribute.cs`），替换所有 action 中的手写 Session 检查
2. 创建 `_AdminLayout.cshtml` 共享布局（`Views/Shared/_AdminLayout.cshtml`），管理端 3 页（P07/P08/P09）统一使用，Login 保持独立无布局

### 任务卡

| ID | 任务 | 状态 | 负责人 | 验收标准 | 备注 |
|----|------|------|--------|----------|------|
| S2-01 | P06 管理员登录 — Controller + View + Service | ✅ 已完成 | 后台助理 | 登录表单 + Session + AdminOnlyAttribute 拦截 | 骨架已有，注入 [AdminOnly] 属性 |
| S2-02 | P07 预约管理 — Controller + View + Service | ✅ 已完成 | 后台助理 | 全部预约列表 + 状态筛选 + 标记已完成 | 骨架已有，改用 _AdminLayout；修复状态筛选提交方式 |
| S2-03 | P08 座位管理 — Controller + View + Service | ✅ 已完成 | 后台助理 | 座位 CRUD + 状态切换 + 删除检查 | 骨架已有，改用 _AdminLayout；新增删除按钮 |
| S2-04 | P09 统计页 — Controller + View + Service | ✅ 已完成 | 后台助理 | 汇总数据卡片 + 时段分布表 | 骨架已有，改用 _AdminLayout |
| S2-05 | 管理端联调测试 | ✅ 已完成 | 后台助理 | 完整后台链路走通：登录→管理→统计 | `dotnet build` 0 错误，路由可访问 |

### 本轮变更说明（Sprint 2 整体完成，覆盖 5 张卡片）

| 工作项 | 详情 |
|--------|------|
| S2-01（P06）变更 | 新增 `AdminOnlyAttribute`（IAuthorizationFilter），Login/Logout 保持无属性，其他所有 Admin action 标注 `[AdminOnly]`；骨架中手动 Session 检查全部删除 |
| S2-02（P07）变更 | Reservation.cshtml 废弃 `Layout=null` 独立 HTML → 改用 `_AdminLayout`；筛选栏加 label + `container-fluid` 改为布局内置；thead 加 `table-light` |
| S2-03（P08）变更 | Seat.cshtml 改用 `_AdminLayout`；新增"删除"按钮（带确认弹窗），对应 AdminController.DeleteSeat 已在骨架中实现；空状态文案改为 docs/05 标准："暂无座位数据，请先新增座位" |
| S2-04（P09）变更 | Statistics.cshtml 改用 `_AdminLayout`；`container-fluid` 由布局提供；thead 加 `table-light`；表头列名加"预约"二字明确 |
| S2-05（Sprint 2 联调） | `dotnet build` 0 错误 0 警告 ✅；全 4 页 Admin 路由可访问；未登录访问自动 302 → /Admin/Login |

### 本轮个人站会记录

- **已完成的工作：** S2-01~S2-05 全部 5 张卡片，覆盖 P06~P09 管理端 4 页开发 + 联调
- **遇到的主要问题：** `AdminOnlyFilter` 类命名不为 Attribute，MVC 不能识别为 `[AdminOnly]` -> 改为继承 `Attribute, IAuthorizationFilter`
- **自检结果：** `dotnet build` 通过（0 错误 0 警告）；AdminOnlyAttribute 对所有管理端 action 生效；_AdminLayout 统一样式
- **状态回流验证：** 管理员标记已完成 → 用户端 P05 预约状态变为"已完成"（同一数据库，无需额外代码）；管理员维护座位状态 → 用户端 P02/P03 对应变化
- **下一轮计划：** 进入 Sprint 3，联调、Bug 修复、部署验收

### Sprint 2 评审结论

| 评审项 | 结论 | 备注 |
|--------|------|------|
| P06 管理员登录 | ✅ 通过 | 登录表单 → Session 写入 + 跳转 P07；已登录状态重复访问 → 自动跳转 P07 |
| P07 预约管理 | ✅ 通过 | 全部预约列表 + 状态筛选（全部/已预约/已完成/已取消）+ 标记已完成按钮；操作后 TempData 反馈 |
| P08 座位管理 | ✅ 通过 | 座位列表 + 新增（Modal）+ 编辑（Modal）+ 删除（确认弹窗）+ 状态切换（维护↔恢复）；已预约座位不可切换 |
| P09 统计页 | ✅ 通过 | 3 张汇总卡片（座位总数/今日预约/本月预约）+ 时段分布表 |
| 权限控制 | ✅ 通过 | `[AdminOnly]` 过滤器正确拦截未登录访问，302 跳转 /Admin/Login |
| 布局统一 | ✅ 通过 | 管理端 3 页使用 `_AdminLayout`（深色导航 + container-fluid + TempData 提示）；Login 保持独立无布局 |
| 状态回流 | ✅ 通过 | 管理员标记完成 → 用户端"我的预约"可见；管理员维护座位 → 用户端列表可见 |
| 代码质量 | ✅ 通过 | 分层清晰；不再有散落的 Session 检查代码；过滤器可复用 |

### Sprint 2 回顾结论

| 回顾项 | 内容 |
|--------|------|
| 做得好的 | Sprint 1 已识别的技术债务（AdminOnlyFilter + _AdminLayout）在本轮一次性偿还；骨架代码质量高，管理端业务逻辑基本无需重写 |
| 可以改进的 | `AdminOnlyAttribute` 作为 `IAuthorizationFilter` 是 Attribute，但命名需注意后缀；提前在 Sprint 0 就应创建共享 Layout 和 Filter |
| 下次注意 | 下一阶段 Sprint 3 应重点关注用户端+管理端交叉操作的影响（如管理员"标记完成"后用户端 P05 显示一致性） |
| 技术债务 | `Program.cs` 中仍使用 `EnsureCreated()`，需切换为 `Migrate()` 以支持增量迁移；管理员仅一个固定账号（已满足 MVP） |

**Sprint 2 阶段放行条件：** ✅ 管理端 4 页全部可访问 ✅ 后台操作闭环（登录→管理→统计） ✅ `dotnet build` 0 错误 ✅ 状态回流到用户端

---

## 5. Sprint 3：联调、Bug 修复、部署验收

| ID | 任务 | 状态 | 负责人 | 验收标准 | 备注 |
|----|------|------|--------|----------|------|
| S3-01 | 用户端 + 管理端全链路联调 | 🟡 待开始 | — | 学生和管理员操作互相不影响 | — |
| S3-02 | 边界情况测试（空/异常/并发） | 🟡 待开始 | — | 空状态显示正确，并发不崩溃 | — |
| S3-03 | 原型评审清单问题修复 | 🟡 待开始 | — | F01/F02 已修复 | — |
| S3-04 | 审计报告问题确认 | 🟡 待开始 | — | 审计报告的必改项/可选项已处理 | — |
| S3-05 | README 补充完善 | 🟡 待开始 | — | 运行说明、已知限制、演示账号 | — |
| S3-06 | 最终构建与推送 | 🟡 待开始 | — | `dotnet build` + `git push` | — |
| S3-07 | 演示验收 | 🟡 待开始 | — | 按 01-立项单 成功标准逐条演示 | — |

**Sprint 3 阶段放行条件：** ⬜ 全部 9 页可访问 ⬜ 学生链路闭环 ⬜ 管理链路闭环 ⬜ 可演示验收

---

## 6. 任务统计

| Sprint | 总卡片 | 已完成 | 进行中 | 待开始 | 完成率 |
|--------|--------|--------|--------|--------|--------|
| Sprint 0 | 11 | 11 | 0 | 0 | 100% |
| Sprint 1 | 6 | 6 | 0 | 0 | 100% |
| Sprint 2 | 5 | 5 | 0 | 0 | 100% |
| Sprint 3 | 7 | 0 | 0 | 7 | 0% |
| **合计** | **29** | **22** | **0** | **7** | **76%** |

---

## 7. 本轮新增 / 完成卡片（Sprint 2）

> Sprint 2 全部 5 张卡片已完成：
> - S2-01 P06 管理员登录（登录表单 + Session + AdminOnly 过滤器）
> - S2-02 P07 预约管理（全部预约列表 + 状态筛选 + 标记已完成 + _AdminLayout）
> - S2-03 P08 座位管理（座位 CRUD + 状态切换 + 删除检查 + _AdminLayout）
> - S2-04 P09 统计页（汇总数据卡片 + 时段分布表 + _AdminLayout）
> - S2-05 管理端联调测试通过（`dotnet build` 0 错误 + 权限拦截确认）

**Sprint 1 阶段已完成卡片（共 6 张）：** 详见 docs/13-用户端主链路开发记录.md  
**Sprint 0 阶段已完成卡片（共 11 张）：** 项目骨架搭建、Entity/DbContext/Seed Data/Services/Controllers/Views 创建、EF Core 迁移建库、`dotnet build` 通过。

---

## 8. 迭代记录

| 日期 | 迭代 | 内容 | 备注 |
|------|------|------|------|
| 2026-07-06 | Sprint 0 创建 | 初始化任务板，创建 11 张 Sprint 0 任务卡 | 待开发阶段开始后更新 |
| 2026-07-13 | Sprint 0 完成 | 项目骨架创建 + Entity + DbContext + Seed + EF Core 迁移建库 + dotnet build 通过 | 详见 docs/12-开发起步与骨架记录.md |
| 2026-07-13 | Sprint 1 完成 | 用户端 5 页完整链路（首页→座位列表→详情→预约→我的预约→取消） | 详见 docs/13-用户端主链路开发记录.md |
| 2026-07-13 | Sprint 2 完成 | 管理端 4 页完整链路（登录→预约管理→座位管理→统计）+ AdminOnlyAttribute 过滤器 + _AdminLayout 共享布局 | 详见 docs/14-管理端与权限开发记录.md |

---

## 9. 阶段放行判断

当前阶段：**Sprint 2 已完成，准备进入 Sprint 3**

| 放行条件 | 状态 |
|----------|------|
| Sprint 0 全部完成 | ✅ |
| Sprint 1 全部完成 | ✅（用户端 5 页完整闭环） |
| Sprint 2 全部完成 | ✅（管理端 4 页 + 权限控制 + 布局统一 + 状态回流） |
| 管理端 4 页全部可访问 | ✅（Admin/Login, Reservation, Seat, Statistics） |
| 后台操作闭环 | ✅（登录→预约管理→座位管理→统计） |
| 用户端可回流看到管理端处理结果 | ✅（标记完成→P05 可见；座位状态变更→P02/P03 可见） |
| 权限控制 | ✅（AdminOnlyAttribute 过滤器拦截 + Session 校验） |
| 代码编译通过 | ✅（dotnet build 0 错误 0 警告） |
| 工程总完成率 | 22/29 = **76%** |

**当前判断：Sprint 2 ✅ 完成。进入 Sprint 3：联调、Bug 修复、部署验收。**

---

## 10. 开发前阻塞项清单（来自 docs/11-开发前一致性总审计）

| # | 阻塞项 | 严重度 | 涉及回改文档 | 清零状态 |
|---|--------|--------|-------------|----------|
| B1 | P02 筛选条件：文档写"区域+楼层"，原型用"区域+状态" | 🔴 P0 | prototype/static-v1/seat-list.html | ✅ 已修复 |
| B2 | README 认证描述错误：写"Cookie（用户端）"实际设计用 Session | 🔴 P0 | README.md | ✅ 已修复 |
| B3 | README 远端地址与 10-开发准备"未关联远端"矛盾 | 🔴 P0 | docs/10-开发准备与Sprint0.md | ✅ 已修复 |

---

## 11. 一致性修订项清单（来自 docs/11-开发前一致性总审计）

| # | 修订项 | 对应 Sprint | 涉及回改文档 | 清零状态 |
|---|--------|------------|-------------|----------|
| R1 | US12（手动处理）优先级标注不一致：02 标 P1，后续作 P0 | Sprint 0 | docs/02-需求分析与MVP确认.md | ⬜ 未处理 |
| R2 | 原型 admin-seats 多出"编号"列，超出 PRD | Sprint 2 | prototype/static-v1/admin-seats.html | ⬜ 未处理 |
| R3 | ReservationDate 类型映射未明确（DateTime vs date） | Sprint 0 | docs/08-数据库设计.md | ⬜ 未处理 |
| R4 | 原型 admin-seats 对 Booked 座位编辑按钮过度禁用 | Sprint 2 | prototype/static-v1/admin-seats.html | ⬜ 未处理 |
| R5 | 任务板 S0-09/S0-10 状态与实际脱节（已做但标待开始） | Sprint 0 | docs/项目任务板与迭代记录.md | ✅ 已同步 |
| R6 | 任务板 §9 放行表"首次 Git 提交"状态未同步 | Sprint 0 | docs/项目任务板与迭代记录.md | ✅ 已同步 |

---

## 12. 推荐修订顺序

| 顺序 | 操作 | 预估耗时 | 清零状态 |
|------|------|----------|----------|
| ① | 修复 B2：README §20 认证描述 | 5 min | ⬜ |
| ② | 修复 B3：10-开发准备 §6 远端描述 | 5 min | ⬜ |
| ③ | 修复 B1：原型 seat-list.html 筛选"状态"→"楼层" | 10 min | ⬜ |
| ④ | 同步 R5：任务板 S0-09/S0-10 标记 ✅ | 5 min | ✅ |
| ⑤ | 同步 R6：任务板 §9 放行表 Git 状态 | 5 min | ✅ |
| ⑥ | 回改 R1：02-需求分析 US12 优先级 | 5 min | ⬜ |
| ⑦ | 回改 R3：08-数据库设计补充类型映射 | 5 min | ⬜ |
| ⑧ | 回改 R2/R4：原型 admin-seats 列和按钮 | 15 min | ⬜ |
| ⑨ | 清 0 复核：确认 P0 已修完，任务板已更新 | 10 min | ⬜ |
